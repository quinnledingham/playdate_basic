# Basic [Playdate](https://play.date/) Application

### How to build and run
Add PLAYDATE_SDK_PATH environment variable
Add to Path the filepath to arm-none-eabi-gcc compiler
Run build in /playdate_basic
Open build folder in Visual Studio
Set hello_world as the Startup Project
F5